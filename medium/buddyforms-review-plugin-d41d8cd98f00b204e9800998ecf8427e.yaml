id: buddyforms-review-plugin-d41d8cd98f00b204e9800998ecf8427e

info:
  name: >
    BuddyForms Moderation <= 1.4.16 - Authenticated Stored Cross-Site Scripting
  author: topscoder
  severity: medium
  description: >
    The BuddyForms Moderation plugin for WordPress is vulnerable to Stored Cross-Site Scripting via the 'buddyforms_moderators' parameter in versions up to, and including, 1.4.16 due to insufficient input sanitization and output escaping. This makes it possible for authenticated attackers with access to edit BuddyForms to inject arbitrary web scripts in pages that will execute whenever a user accesses an injected page.
  reference:
    - https://www.wordfence.com/threat-intel/vulnerabilities/id/50c8a20a-66b4-445e-9167-e6fc0e6a1000?source=api-prod
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:L/I:L/A:N
    cvss-score: 6.4
    cve-id: 
  metadata:
    fofa-query: "wp-content/plugins/buddyforms-review/"
    google-query: inurl:"/wp-content/plugins/buddyforms-review/"
    shodan-query: 'vuln:'
  tags: cve,wordpress,wp-plugin,buddyforms-review,medium

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/wp-content/plugins/buddyforms-review/readme.txt"

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        internal: true
        regex:
          - "(?mi)Stable tag: ([0-9.]+)"

      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - "(?mi)Stable tag: ([0-9.]+)"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        words:
          - "buddyforms-review"
        part: body

      - type: dsl
        dsl:
          - compare_versions(version, '<= 1.4.16')